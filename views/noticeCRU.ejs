<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    * {
      font-family: "Noto Sans KR", sans-serif;
    }
  </style>
</head>
<body>
  <% if (title === "공지사항 수정") { %>
    <div class="max-w-4xl mx-auto p-6">
      <h1 class="text-2xl font-semibold text-gray-900 mb-4">공지사항 수정</h1>
      <form action="/admin/notice/<%= results.id %>" method="POST">
        <input type="hidden" name="_method" value="PUT">
        <div class="mb-4">
          <label for="title" class="block text-lg font-medium text-gray-700">제목</label>
          <input type="text" id="title" name="title" value="<%= results.title %>" class="mt-1 p-2 border border-gray-300 rounded w-full" required>
        </div>
        <div class="mb-4">
          <label for="content" class="block text-lg font-medium text-gray-700">내용</label>
          <textarea id="content" name="content" rows="6" class="mt-1 p-2 border border-gray-300 rounded w-full" required><%= results.content %></textarea>
        </div>
        <div class="mb-4">
          <label for="pin" class="block text-lg font-medium text-gray-700">고정 여부</label>
          <select id="pin" name="pin" class="mt-1 p-2 border border-gray-300 rounded w-full">
            <option value="true" <%= results.pin ? 'selected' : '' %>>핀 고정</option>
            <option value="false" <%= !results.pin ? 'selected' : '' %>>일반</option>
          </select>
        </div>
        <div class="mt-4 text-center">
          <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">수정 완료</button>
        </div>
      </form>
      
  
      <div class="mt-4 text-center">
        <a href="/admin/notice">
          <button type="button" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 transition">돌아가기</button>
        </a>
      </div>
    </div>
  <% } else if (title === "공지사항 생성") { %>
    <div class="max-w-4xl mx-auto p-6">
      <form action="/admin/notice/make" method="POST">
        <div class="mb-4">
          <label for="title" class="block text-lg font-medium text-gray-700">제목</label>
          <input type="text" id="title" name="title" class="mt-1 p-2 border border-gray-300 rounded w-full" required>
        </div>
        
        <div class="mb-4">
          <label for="content" class="block text-lg font-medium text-gray-700">내용</label>
          <textarea id="content" name="content" rows="6" class="mt-1 p-2 border border-gray-300 rounded w-full" required></textarea>
        </div>
        
        <div class="mb-4">
          <label for="pin" class="block text-lg font-medium text-gray-700">고정 여부</label>
          <select id="pin" name="pin" class="mt-1 p-2 border border-gray-300 rounded w-full">
            <option value="true">핀 고정</option>
            <option value="false">일반</option>
          </select>
        </div>
      
        <div class="mt-4 text-center">
          <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">생성 완료</button>
        </div>
      </form>
      <div class="mt-4 text-center">
        <a href="/admin/notice">
          <button type="button" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 transition">돌아가기</button>
        </a>
      </div>
    </div>
  <% } %>
</body>
</html>
